# Error Codes

> Purpose: HTTP status semantics, error_code enum, WebSocket close codes 4001/4003, and rate limit headers.

<!-- Populate from: backend/app/api/v1/, backend/app/core/exceptions.py (verify path) -->

## HTTP Error Shape

```json
{
  "detail": "Human-readable message",
  "error_code": "MACHINE_READABLE_CODE"
}
```

## HTTP Status Codes

| Status | Meaning | Common Causes |
|--------|---------|--------------|
| `400` | Bad Request | Validation error, malformed JSON |
| `401` | Unauthorized | Missing/invalid/expired Bearer token |
| `403` | Forbidden | Valid token but insufficient permissions (e.g., not session owner) |
| `404` | Not Found | Resource does not exist or was deleted |
| `409` | Conflict | Duplicate resource (e.g., email already registered) |
| `422` | Unprocessable Entity | Pydantic validation failure (auto-generated by FastAPI) |
| `429` | Too Many Requests | Rate limit exceeded — see rate limit headers below |
| `500` | Internal Server Error | Unhandled exception; check logs |
| `503` | Service Unavailable | DB or Redis connection lost |

## error_code Enum

| error_code | HTTP Status | Description |
|------------|-------------|-------------|
| `INVALID_CREDENTIALS` | 401 | Wrong email/password on login |
| `TOKEN_EXPIRED` | 401 | JWT access token has expired |
| `TOKEN_BLACKLISTED` | 401 | Logged-out token reused |
| `SESSION_NOT_OWNED` | 403 | Attempting to access another teacher's session |
| `SESSION_NOT_FOUND` | 404 | Session ID does not exist |
| `YOUTUBE_QUOTA_EXCEEDED` | 503 | YouTube API quota exhausted |
| <!-- add remaining --> | | |

## WebSocket Close Codes

| Code | Meaning |
|------|---------|
| `4001` | Invalid or expired JWT token in `?token=` parameter |
| `4003` | Valid token but user does not own the requested session |

## Rate Limiting

When rate limited (HTTP 429), the response includes:

| Header | Description |
|--------|-------------|
| `X-RateLimit-Limit` | Max requests per window |
| `X-RateLimit-Remaining` | Requests left in current window |
| `X-RateLimit-Reset` | Unix timestamp when the window resets |
| `Retry-After` | Seconds to wait before retrying |

<!-- Populate: rate limit windows per endpoint group if they differ -->
